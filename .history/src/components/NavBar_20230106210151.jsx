import React from "react";

const NavBar = () => {
    const links = {'Home', 'Setup', 'Goals', 'Contacts'}
    return (
      <>
        <nav>

        </nav>
      </>
    );
};

export default NavBar;